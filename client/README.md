# Best XI — Client

React + Vite frontend for the squad builder game.

## Setup

```bash
npm install
npm run dev
```

The Vite dev server runs at `http://localhost:5173` and proxies `/api` and `/images` to the Express backend on port 5000.

## Build

```bash
npm run build
```

Outputs to `dist/`.
